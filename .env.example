# Server
PORT=3000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:3000

# Firebase Admin SDK
# Option A: point to your downloaded Firebase service account JSON.
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# NEVER put serviceAccountKey.json into GitHub.
