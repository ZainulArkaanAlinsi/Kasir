{
  "name": "kasirone",
  "version": "1.0.0",
  "description": "KasirOne - sistem kasir & inventory toko retail (Express + Firebase)",
  "type": "module",
  "main": "server/server.js",
  "scripts": {
    "dev": "node --watch server/server.js",
    "start": "node server/server.js",
    "test": "node --test test/money.test.js test/report.test.js test/demo.test.js test/payment.test.js test/catalog.test.js",
    "test:emulator": "firebase emulators:exec --only firestore \"node --test --test-concurrency=1 test/transaction.emulator.test.js test/reservation.emulator.test.js test/order.emulator.test.js\"",
    "test:rules": "firebase emulators:exec --only firestore \"node --test test/rules.test.js\"",
    "test:all": "npm run test && npm run test:emulator && npm run test:rules",
    "set-role": "node scripts/set-role.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "firebase-admin": "^13.5.0",
    "helmet": "^8.1.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^13.35.1"
  }
}
